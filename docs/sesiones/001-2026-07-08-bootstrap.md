# Sesión 001 — 2026-07-08 — Bootstrap de infraestructura IA

## Qué se hizo

- **Reorganización del código**: la v3 real (7,003 líneas, la que usa la
  tienda) se movió de `ando-haciendo-un-proyecto-de-un/pos_abarrotes.py` a
  la raíz como `pos_abarrotes.py` (fuente canónica) junto con `assets/`.
  La v2 vieja (`build/pos_abarrotes.py`) quedó sin trackear.
- **Limpieza del índice de git**: se destrackearon (siguen en disco) el
  `.exe`, `abarrotes_pos.db`, `abarrotes_pos_respaldo.db`, `__pycache__/`,
  `build/` y `dist/`. `.gitignore` ampliado (`.env.*`, `*.pem`, `*.key`,
  `.claude/settings.local.json`, `*.zip`, `*.rar`, `.pytest_cache/`,
  `ando-haciendo-un-proyecto-de-un/`).
- **Arnés**: `CLAUDE.md` · `.mcp.json` (Context7) · skills `esquema-bd`,
  `reglas-caja`, `convenciones-ui` · comandos `/test`, `/arregla-tests`,
  `/nueva-feature`, `/deploy-check`, `/cierre-sesion` · agentes
  `implementador` (sonnet) y `verificador` (sin edición).
- **SDD**: `spec/constitution/` (mission, tech-stack, roadmap con 4
  features) + `spec/features/000-plantilla/` (spec, plan, tasks).
- **Tests**: `requirements.txt`, `tests/conftest.py` (BD temporal) y
  `tests/test_logica.py` — **10 tests, todos en verde**, incluida la
  fórmula del corte de caja (`esperado = fondo + efectivo + anticipos −
  devoluciones`).
- **Docs**: `docs/README.md`, `docs/guia-prompts.md`,
  `docs/guia-ejecucion.md`, `docs/checklist-seguridad.md`,
  `docs/sesiones/000-plantilla.md` y esta bitácora.

## Decisiones

- Fuente canónica: `pos_abarrotes.py` en la raíz (v3). La carpeta
  `ando-haciendo-un-proyecto-de-un/` es histórica: no se edita y quedó
  ignorada por git (los archivos siguen en disco).
- Idioma español · flujo estricto · SDD spec-anchored (ya en CLAUDE.md).
- Roadmap priorizado por riesgo: reportes → contraseñas → granel kg/gr →
  multi-equipo (ver `spec/constitution/roadmap.md`).

## Pendiente / siguiente paso

1. **Seguridad (abierto):** el HISTORIAL de git aún contiene las BDs
   reales y el `.exe` (commit "Primera version POS"). Decidir: reescribir
   historial con `git filter-repo` + force-push, o volver privado el repo.
   Ver `docs/checklist-seguridad.md`.
2. Cambiar las contraseñas por defecto en la instalación real de la tienda.
3. Confirmar prioridades del roadmap y arrancar la feature #1 con
   `/nueva-feature reportes-ampliados`.

## Estado del repo

- **Rama:** main
- **Sin commitear:** toda la infraestructura del bootstrap + reorganización
  (pendiente de commit «Infraestructura de agente»)
- **Tests:** 10/10 en verde (`py -m pytest`)
